# Restic environment variables for R2 backup
# Auto-generated by Ansible - DO NOT EDIT MANUALLY

# R2 S3-compatible endpoint
export AWS_ACCESS_KEY_ID="{{ r2_backup_access_key_id }}"
export AWS_SECRET_ACCESS_KEY="{{ r2_backup_secret_access_key }}"
export RESTIC_REPOSITORY="s3:{{ r2_endpoint_url }}/{{ r2_backup_bucket_name }}"

# Restic encryption password (IMPORTANT: store this safely!)
export RESTIC_PASSWORD="{{ restic_password }}"

# Optional: Cache directory
export RESTIC_CACHE_DIR="/opt/opticworks/backup/.cache"
