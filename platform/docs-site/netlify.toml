[build]
  command = "pnpm docs:build"
  publish = "platform/docs-site/public"

[build.environment]
  HUGO_VERSION = "0.125.0"
  HUGO_ENABLEGITINFO = "true"

[context.deploy-preview.environment]
  HUGO_ENV = "development"
